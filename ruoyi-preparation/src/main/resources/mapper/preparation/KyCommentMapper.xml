<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.preparation.mapper.KyCommentMapper">
    
    <resultMap type="com.ruoyi.preparation.domain.KyComment" id="KyCommentResult">
        <id     property="id"           column="id"           />
        <result property="targetId"     column="target_id"    />
        <result property="targetType"   column="target_type"  />
        <result property="parentId"     column="parent_id"    />
        <result property="userId"       column="user_id"      />
        <result property="content"      column="content"      />
        <result property="delFlag"      column="del_flag"     />
        <result property="createTime"   column="create_time"  />
        <result property="userName"     column="user_name"    />
        <result property="userAvatar"   column="avatar"       />
    </resultMap>

    <sql id="selectKyCommentVo">
        select c.id, c.target_id, c.target_type, c.parent_id, c.user_id, c.content, 
               c.del_flag, c.create_time, u.nick_name as user_name, u.avatar
        from ky_comment c
        left join sys_user u on c.user_id = u.user_id
    </sql>

    <select id="selectKyCommentList" parameterType="com.ruoyi.preparation.domain.KyComment" resultMap="KyCommentResult">
        <include refid="selectKyCommentVo"/>
        <where>
            c.del_flag = '0'
            <if test="targetId != null"> and c.target_id = #{targetId}</if>
            <if test="targetType != null  and targetType != ''"> and c.target_type = #{targetType}</if>
            <if test="parentId != null"> and c.parent_id = #{parentId}</if>
            <if test="userId != null"> and c.user_id = #{userId}</if>
            <if test="content != null  and content != ''"> and c.content like concat('%', #{content}, '%')</if>
        </where>
        order by c.create_time desc
    </select>

    <select id="selectCommentsByTarget" resultMap="KyCommentResult">
        <include refid="selectKyCommentVo"/>
        where c.del_flag = '0' and c.target_id = #{targetId} and c.target_type = #{targetType}
        order by c.create_time asc
    </select>
    
    <select id="selectKyCommentById" parameterType="Long" resultMap="KyCommentResult">
        <include refid="selectKyCommentVo"/>
        where c.id = #{id} and c.del_flag = '0'
    </select>

    <select id="countByTarget" resultType="int">
        select count(1) from ky_comment 
        where del_flag = '0' and target_id = #{targetId} and target_type = #{targetType}
    </select>
        
    <insert id="insertKyComment" parameterType="com.ruoyi.preparation.domain.KyComment" useGeneratedKeys="true" keyProperty="id">
        insert into ky_comment
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="targetId != null">target_id,</if>
            <if test="targetType != null">target_type,</if>
            <if test="parentId != null">parent_id,</if>
            <if test="userId != null">user_id,</if>
            <if test="content != null">content,</if>
            <if test="delFlag != null">del_flag,</if>
            create_time
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="targetId != null">#{targetId},</if>
            <if test="targetType != null">#{targetType},</if>
            <if test="parentId != null">#{parentId},</if>
            <if test="userId != null">#{userId},</if>
            <if test="content != null">#{content},</if>
            <if test="delFlag != null">#{delFlag},</if>
            sysdate()
        </trim>
    </insert>

    <update id="updateKyComment" parameterType="com.ruoyi.preparation.domain.KyComment">
        update ky_comment
        <trim prefix="SET" suffixOverrides=",">
            <if test="targetId != null">target_id = #{targetId},</if>
            <if test="targetType != null">target_type = #{targetType},</if>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="content != null">content = #{content},</if>
            <if test="delFlag != null">del_flag = #{delFlag},</if>
        </trim>
        where id = #{id}
    </update>

    <update id="deleteKyCommentById" parameterType="Long">
        update ky_comment set del_flag = '1' where id = #{id}
    </update>

    <update id="deleteKyCommentByIds" parameterType="String">
        update ky_comment set del_flag = '1' where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>
</mapper>
